# Database (Drizzle / Postgres) — optional until you use DB
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lilo_wms

# API
API_PORT=4000
API_HOST=0.0.0.0
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=change-me-in-production-min-32-chars-please-xxxxxxxx
JWT_EXPIRES_IN=7d

# Web
NEXT_PUBLIC_API_URL=http://localhost:4000

# Worker / BullMQ (Redis) — optional in dev; worker uses lazy connect
REDIS_URL=redis://127.0.0.1:6379
WORKER_CONCURRENCY=5
