/**
 * Mantém o horário como exibido no mock estático.
 * Quando houver backend com ISO, converter aqui para o formato de exibição.
 */
export const formatHoraChegada = (hora: string): string => hora;
